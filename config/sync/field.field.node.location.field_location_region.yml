uuid: c38fe695-b220-4ae5-8835-7bedcc408d8e
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_location_region
    - node.type.location
    - node.type.region
id: node.location.field_location_region
field_name: field_location_region
entity_type: node
bundle: location
label: Region
description: 'Select any region in which this location can be found. This field allows multiple selections because regions can overlap. A location will not appear on a region map unless that region is selected here.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      region: region
    sort:
      field: title
      direction: ASC
    auto_create: true
    auto_create_bundle: ''
field_type: entity_reference
